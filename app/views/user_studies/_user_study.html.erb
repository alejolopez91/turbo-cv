<div class="mb-2">
  <p class="font-semibold">Degree:</p>
  <p><%= study.name %></p>
</div>
<div class="mb-2">
  <p class="font-semibold">Institution:</p>
  <p><%= study.school_name %></p>
</div>
<div class="mb-2">
  <p class="font-semibold">Year:</p>
  <p><%= study.started_at %> to <%= study.finished_at || 'current' %></p>
</div>